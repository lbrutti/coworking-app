import { LangToFlagPipe } from './lang-to-flag.pipe';

describe('LangToFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new LangToFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
